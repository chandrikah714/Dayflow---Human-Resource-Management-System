import { useState } from 'react';
import { useAuth } from '../../auth/AuthContext';
import './Login.css';
export default function Login() {
  const { login, register } = useAuth(); const [signUp, setSignUp] = useState(false); const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false);
  async function submit(event) { event.preventDefault(); try { setBusy(true); setError(''); if (signUp) await register(name, email, password); else await login(email, password); } catch (requestError) { setError(requestError.message); } finally { setBusy(false); } }
  return <main className="login-page"><div className="login-orb login-orb--one" /><div className="login-orb login-orb--two" /><section className="login-card"><div className="login-brand"><span className="login-brand__mark">D</span><span>Dayflow <small>HRMS</small></span></div><div className="login-kicker">{signUp ? 'GET STARTED' : 'SECURE WORKSPACE'}</div><h1>{signUp ? 'Build your workspace' : 'Welcome back'}</h1><p>{signUp ? 'Set up your Dayflow account in less than a minute.' : 'Sign in to manage your people, time and workday.'}</p><form className="login-form" onSubmit={submit}>{signUp && <label>Full name<input required autoComplete="name" placeholder="Your full name" value={name} onChange={(e) => setName(e.target.value)} /></label>}<label>Work email<input required type="email" placeholder="name@company.com" autoComplete="email" value={email} onChange={(e) => setEmail(e.target.value)} /></label><label>Password<input required minLength="8" type="password" placeholder="At least 8 characters" autoComplete={signUp ? 'new-password' : 'current-password'} value={password} onChange={(e) => setPassword(e.target.value)} /></label><button className="login-submit" disabled={busy}>{busy ? 'Please wait…' : signUp ? 'Create account' : 'Sign in to Dayflow'}<span>→</span></button></form>{error && <p className="login-error" role="alert">{error}</p>}<div className="login-divider"><span>or</span></div><button type="button" className="login-switch" onClick={() => setSignUp(!signUp)}>{signUp ? 'Already have an account? Sign in' : 'New to Dayflow? Create an account'}</button></section><p className="login-footnote">Private, role-aware HR operations for modern teams.</p></main>;
}
