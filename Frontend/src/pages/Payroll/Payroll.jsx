/* eslint-disable react-hooks/exhaustive-deps */
import { useEffect, useState } from 'react';
import { useAuth } from '../../auth/AuthContext';
export default function Payroll() { const { user } = useAuth(); const [rows, setRows] = useState([]); const [error, setError] = useState(''); const load = () => fetch(user.role === 'ADMIN' ? '/api/payroll' : '/api/payroll/me', { credentials: 'include' }).then(async (r) => { if (!r.ok) throw new Error(await r.text()); return r.json(); }).then((data) => setRows(Array.isArray(data) ? data : [data])).catch((e) => setError(e.message)); useEffect(() => { void load(); }, []); return <main className="dashboard"><h1>Payroll</h1><p>Your current compensation summary.</p>{error && <p role="alert">{error}</p>}<section className="dashboard__team"><div className="attendance-table-container"><table className="attendance-table"><thead><tr><th>Employee ID</th><th>Basic</th><th>HRA</th><th>Allowances</th><th>Deductions</th><th>Net salary</th></tr></thead><tbody>{rows.map((r) => <tr key={r.id || r.employeeId}><td>{r.employeeId}</td><td>{r.basic}</td><td>{r.hra}</td><td>{r.allowances}</td><td>{r.deductions}</td><td>{r.netSalary}</td></tr>)}</tbody></table></div></section></main>; }
