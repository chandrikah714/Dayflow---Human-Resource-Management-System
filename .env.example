# Base URL of your Spring Boot backend. The frontend calls
# `${VITE_API_BASE_URL}/auth/login`, `/employee/profile`, etc.
# Leave unset (or unreachable) and the app runs on demo data automatically.
VITE_API_BASE_URL=http://localhost:8080/api
